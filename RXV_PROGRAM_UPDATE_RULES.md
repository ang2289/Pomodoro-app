# RXV 程式開發／更新固定規則

## 核心原則
1. **最快可用優先**
   - 先做最小可用版本（MVP）。
   - 不先加入非必要架構、設定頁、安裝流程或複雜選項。
   - 能一個按鈕完成，就不要拆成多個手動步驟。

2. **更新流程固定為最短路徑**
   - 預設交付方式：`下載 → 解壓 → 雙擊 ONECLICK.bat`
   - 已知安裝路徑時直接使用，不再要求重複選資料夾。
   - 只有找不到既有路徑時才要求人工選擇。

3. **更新前自動備份**
   - 每次更新前自動備份舊程式檔。
   - 備份資料夾名稱需含版本與時間。
   - 更新失敗時可快速回復。

4. **永遠保留既有資料**
   - 不可因更新清空：
     - `data`
     - 使用者設定
     - 歷史紀錄
     - 掃描結果
     - queue / decisions
     - 專案清單
     - 來源 / 目的地 / 排除設定
   - 能沿用既有索引、掃描、快取就不要重跑。

5. **最小修改**
   - 優先修改最少檔案。
   - 不為了小功能重構整個專案。
   - 不任意更換框架、資料庫、套件或目錄結構。

6. **Windows 穩定優先**
   - 優先使用簡單 `.bat`。
   - 沒必要就不要用 PowerShell。
   - 若必須用 PowerShell，要避免中文編碼造成 Windows PowerShell 5.1 解析失敗。
   - 批次檔優先使用 ASCII / CRLF，相容舊 Windows 環境。

7. **交付前必須測試**
   - 至少檢查：
     - 語法
     - 啟動
     - 主要按鈕 / API
     - 更新後舊資料是否仍存在
   - 不把未測過的升級腳本直接交付。

8. **零成本優先**
   - 優先：
     - 本機模型
     - 免費工具
     - 既有服務
   - 任何可能產生 API、雲端、訂閱或按量費用的功能：
     - 必須先說明
     - 未經使用者同意不得預設開啟

9. **先穩定，再加進階功能**
   - 第一版只解決目前最重要問題。
   - 基本流程成功後，再做：
     - 自動化
     - AI
     - 批次
     - 報表
     - 多模型
     - 雲端整合

10. **每次交付只給最短操作**
    - 回覆預設只提供：
      1. 下載連結
      2. 放哪裡 / 是否覆蓋
      3. 雙擊哪個檔案
      4. 成功後應看到什麼
    - 除非出錯，不先給大量備用指令。

## RXV 專案更新標準
若目前已知 RXV 安裝位置，預設直接更新現有位置，不重新安裝新副本。

更新包命名：
- `RXV-vX.X.X-oneclick.zip`

升級檔命名：
- `UPGRADE-RXV-vX.X.X-ONECLICK.bat`

執行流程：
1. 關閉舊程式
2. 雙擊 ONECLICK
3. 自動備份
4. 只覆蓋程式檔
5. 保留資料
6. 啟動新版
7. 驗證版本

## AI / API 規則
- 本機 Ollama 優先。
- 不自動切回 OpenAI / Gemini / Groq 等外部 API。
- fallback 不可默默指向付費模型。
- 若功能需要付費 API，先停止實作並詢問使用者是否接受成本。

## 判斷準則
每次寫程式前先問自己：
>「有沒有更簡單、更快、更不容易壞的做法？」

若有，優先用更簡單的做法。
