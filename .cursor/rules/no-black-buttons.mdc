---
description: 按鈕避免使用黑色／近黑底色
globs: "**/*.{tsx,jsx,vue,css}"
---

# 按鈕顏色

- **不要**使用純黑或接近純黑作為按鈕底色（例如 `bg-black`、`bg-slate-900`、`bg-slate-800`、`bg-neutral-900`）。
- 選取態、主按鈕請改用品牌色或深藍系（例如 `bg-blue-600`、`bg-indigo-600`），與頁面既有主色一致為佳。
- 一般內文標題、說明文字使用深灰字色（`text-slate-800` 等）不在此限；本規則針對**可點擊的按鈕／按鈕式切換**。
