import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pomodoro.app',
  appName: 'RxV AI 工具與生活服務中心',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;

