# 集氣願望牆功能相關資料表結構

## 1. chant_wishes 表（集氣願望主表）

| 欄位名稱 | 資料型別 | 是否必填 | 說明 |
|---------|---------|---------|------|
| id | uuid | 是 | 主鍵，唯一識別碼 |
| wish_no | integer | 是 | 願望編號（可能為自動遞增序號） |
| title | text | 是 | 願望標題 |
| chant_text | text | 是 | 念誦經文內容 |
| chant_target_count | integer | 是 | 目標念誦次數 |
| chant_unit | text | 是 | 念誦單位（如「遍」、「次」等） |
| for_person_name | text | 否 | 為誰祈福（可選） |
| start_date | date/timestamp | 是 | 活動開始日期 |
| end_date | date/timestamp | 是 | 活動結束日期 |
| description | text | 否 | 願望描述（可選） |
| created_by | text | 是 | 建立者名稱 |
| created_at | timestamp | 是 | 建立時間（通常自動產生） |
| user_id | uuid | 否 | 使用者 ID（可選） |
| image_url | text | 否 | 願望圖片 URL（可選，可為 null） |

---

## 2. chant_wish_supports 表（愛心支持表）

| 欄位名稱 | 資料型別 | 是否必填 | 說明 |
|---------|---------|---------|------|
| id | uuid | 是 | 主鍵，唯一識別碼 |
| chant_wish_id | uuid | 是 | 外鍵，關聯到 chant_wishes.id |
| created_at | timestamp | 是 | 建立時間（通常自動產生） |

**注意**：此表用於記錄用戶對願望的愛心支持（點讚），每次支持新增一筆記錄。

---

## 3. chant_wish_lights 表（點燈記錄表）

| 欄位名稱 | 資料型別 | 是否必填 | 說明 |
|---------|---------|---------|------|
| id | uuid | 是 | 主鍵，唯一識別碼 |
| chant_wish_id | uuid | 是 | 外鍵，關聯到 chant_wishes.id |
| user_name | text | 否 | 點燈者名稱（可為 null，預設為「匿名善信」） |
| message | text | 否 | 點燈留言訊息（可選，可為 null） |
| created_at | timestamp | 是 | 建立時間（通常自動產生） |

**注意**：此表用於記錄用戶為願望點燈祈福的記錄。

---

## 4. chant_logs 表（念誦記錄表）

| 欄位名稱 | 資料型別 | 是否必填 | 說明 |
|---------|---------|---------|------|
| id | uuid | 是 | 主鍵，唯一識別碼 |
| wish_id | uuid | 是 | 外鍵，關聯到 chant_wishes.id |
| user_name | text | 是 | 念誦者名稱 |
| chanted_count | integer | 是 | 念誦次數 |
| created_at | timestamp | 是 | 建立時間（通常自動產生） |

**注意**：此表用於記錄用戶為願望念誦經文的次數記錄。

---

## 5. chant_comments 表（留言評論表）

| 欄位名稱 | 資料型別 | 是否必填 | 說明 |
|---------|---------|---------|------|
| id | uuid | 是 | 主鍵，唯一識別碼 |
| wish_id | uuid | 是 | 外鍵，關聯到 chant_wishes.id |
| user_name | text | 是 | 留言者名稱（可為「匿名」） |
| comment | text | 是 | 留言內容 |
| created_at | timestamp | 是 | 建立時間（通常自動產生） |

**注意**：此表用於記錄用戶對願望的留言評論。

---

## 表關聯關係

```
chant_wishes (主表)
  ├── chant_wish_supports (1對多：一個願望可有多個支持記錄)
  ├── chant_wish_lights (1對多：一個願望可有多個點燈記錄)
  ├── chant_logs (1對多：一個願望可有多筆念誦記錄)
  └── chant_comments (1對多：一個願望可有多筆留言)
```

**外鍵關係**：
- `chant_wish_supports.chant_wish_id` → `chant_wishes.id`
- `chant_wish_lights.chant_wish_id` → `chant_wishes.id`
- `chant_logs.wish_id` → `chant_wishes.id`
- `chant_comments.wish_id` → `chant_wishes.id`

---

## 備註

1. 所有表的主鍵 `id` 欄位通常由 Supabase 自動產生（UUID 格式）。
2. 所有表的 `created_at` 欄位通常由 Supabase 自動設定為當前時間戳。
3. 資料型別說明：
   - `uuid`: PostgreSQL UUID 型別
   - `text`: PostgreSQL TEXT 型別
   - `integer`: PostgreSQL INTEGER 型別
   - `timestamp`: PostgreSQL TIMESTAMP 型別
   - `date`: PostgreSQL DATE 型別
